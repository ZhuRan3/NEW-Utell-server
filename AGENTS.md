# 服务器端 Agent 工作规则

## 任务范围

本仓库只负责 Zhu3xx 云 Relay：身份、设备配对、公钥登记、单主 Connector、撤销、WSS 密文路由、ACK、重连、限流、最小脱敏审计、健康检查和云上部署运维。技术栈已确认：Go + Caddy + modernc.org/sqlite（纯 Go）+ 阿里云（Q-SV-2026-014）。

手机端 UI、手机安全存储、电脑 Connector、Pi Agent、权威数据库和电脑端安装运维不在本仓库实现。跨端联调只消费 `文档/架构与契约/` 和 `harness/契约/` 的版本化公开契约。

## 必读顺序

```text
AGENTS.md
  -> 项目总体背景愿景目标/PRD：Utell（油条）第一代智能日志 MVP.md
  -> 文档/背景与链路/背景复核方法与工作链路.md
  -> 文档/架构与契约/架构基线.md
  -> 文档/架构与契约/跨端公开契约治理规范.md
  -> 文档/工作进度/开发进度与会话交接.md
  -> 开发规则规范/README.md
  -> 开发规则规范/07-人类可读进度与技术沟通规范.md
  -> 文档/交流记录/README.md
  -> 文档/交流记录/项目驾驶舱.md
  -> 当前任务相关的需求、Q&A、契约和 harness 场景
```

## 权威性

1. 用户明确回答并写入 Q&A 的结论高于工程建议。
2. 当前 PRD 高于未写入本仓库的外部材料。
3. 背景复核文件只说明方法，不代表任何外部结论已被采用。
4. 文档、契约和运行时行为不一致时先登记冲突，不静默修正。

## 硬约束

- Relay 永远不能读取、解密或持有端点私钥和业务解密密钥。
- Relay ACK 只表示路由层收到，不得表示 Connector 持久接收、事务提交或 Projection。
- 不建立离线业务队列；无法即时投递的密文按稳定错误码丢弃并告知发送端。
- pairing、撤销、重放、篡改、过期、限流和消息大小必须有契约测试与证据。
- 不实现通用 Shell/HTTP 代理、Agent 执行器或 PC 权威业务。
- 机读契约、fixtures 和证据可共享；不共享手机/Connector 运行时领域代码。
- 两端 `harness/契约/integration-profile.yaml` 必须逐字节一致；契约变更按跨端公开契约治理规范执行。

## 文档要求

需求变更先写 `文档/需求对齐/需求问答记录.md`，保留用户原文、选项、回答和影响；再同步 PRD、架构说明、契约、场景和变更记录。中文说明优先，协议字段、代码标识和命令保留原文。

## 人类可读进度协议

涉及多步骤任务、跨端联调、Spike、Harness、部署或发布时，遵循 `文档/交流记录/可复用模板/AI协作进度协议.md`。会话开始先用“项目驾驶舱”说明当前阶段、唯一主任务、阻塞和下一站；会话结束补齐“测试站证据”，说明测试目的、条件、结果、证据位置、可证明范围和限制。专业名词不得全部口语化或省略：首次出现时保留中文、英文全称或缩写，并给出白话定义、本项目例子、忽略后果和当前证据状态；驾驶舱每次最多引入 1–3 个新术语，必要时补技术学习卡。状态必须区分 `已证实`、`待证据`、`待决策`、`待开发`；涉及范围、安全、协议、数据、兼容性或成本的事项，一次只向用户提出一个确认问题，未确认不得自行落地。
