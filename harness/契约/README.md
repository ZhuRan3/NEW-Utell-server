# Harness 契约目录

`integration-profile.yaml` 是两端逐字节一致的公开契约入口。这里保存经 Q&A、Spike 和跨端 Owner 接受后的 Relay Transport、Connector Service 消费边界、错误码、版本兼容范围和 E2EE 测试向量索引。具体治理、版本和同步规则见 `文档/架构与契约/跨端公开契约治理规范.md`；`pending` 契约不得直接驱动生产代码。
