# 开发进度与会话交接

> 更新时间：2026-08-27
> 当前阶段：G1 当前批次已完成，G2 技术 Spike 进行中;Connector 公开面文档已起草待确认
> 维护规则：每个工作会话结束更新；本文件只记录事实、证据和下一步，不记录未批准的默认方案。所有会话汇报和更新必须同时遵守 `开发规则规范/07-人类可读进度与技术沟通规范.md`，并同步本端 `文档/交流记录/项目驾驶舱.md`。

## 当前阶段门

| 阶段 | 状态 | 完成条件 |
| --- | --- | --- |
| G0 真源同步 | 已完成第一轮 | 两端 PRD v0.5 一致，已确认语义已回写台账/Q&A/基线 |
| G1 需求与决策对齐 | 当前批次完成 | 已关闭本轮产品语义与技术参数；正式公开字段仍需 Connector 共同冻结 |
| G2 技术 Spike | 进行中 | Phone Noise Spike 有结果；Server 本机合成门禁已完成，ECS 容量/背压 Spike 有阈值和证据 |
| G3 integration-profile 冻结 | 待开始 | 两端契约状态 approved 且校验和一致 |
| G4 Harness P0 | 待开始 | P0 场景可重复运行并生成脱敏证据 |
| G5 生产工程骨架 | 待开始 | Phone SwiftUI iOS17 工程和 Server Go Relay 工程可构建 |

## 已完成

- 2026-08-27:起草《Connector 职责边界与公开接口范围》v0.1(草案 pending,最小公开面),两端仓库逐字节一致(SHA-256 `6fd64bf4…a814d`);归并 PRD v0.5 与台账已确认语义,列出 9 项待确认清单(Owner 未定,逐项走 Q&A)。
- 2026-08-27:两端仓库全部工作区改动提交并推送 GitHub(phone `c6f4761`、server `3a98b65`);6 个 ready 场景全套(fixture/runner/evidence)与规范 07、交流记录入库。
- 两端 PRD 同步到 v0.5。
- 原文追溯保留在 P0，在线按需读取、仅内存展示、不缓存、不自动重试，已登记 Q-PH/SV-2026-019。
- 原文追溯公开请求已确认为 `event_id` only；`capture_id`/`log_entry_id` 不进入 Phone/Relay 公开契约，已登记 Q-PH/SV-2026-020。
- Noise Pattern 已确认为 `Noise_XX_25519_ChaChaPoly_SHA256`（Q-SV-2026-021）；Phone/Rust snow 固定向量、transport 和安全失败证据已完成，Connector 共同验收和正式契约写入仍待完成。
- Noise 角色已确认：Phone Initiator、Connector Responder（Q-SV-2026-022）。静态密钥生成、端点安全存储和公钥指纹与 `pairing_id` 绑定方案已确认（Q-SV-2026-023）；P0 rekey 策略也已确认仅在会话重建时完整握手（Q-SV-2026-024）；每会话每方向单调 `uint64` 序号和重连重置已确认（Q-SV-2026-025）；固定总超时、`HANDSHAKE_FAILED` 脱敏错误映射和 `10 秒` 初始值已确认（Q-SV-2026-026/027）；前台指数退避策略已确认（Q-SV-2026-028）；后台不保持长连接、回前台恢复连接已确认（Q-SV-2026-029）；网络恢复不跳过当前退避、`1 秒` 去抖窗口和网络类型切换重握手已确认（Q-SV-2026-030/031/032）；前台无限次重连且退避封顶 `16 秒` 已确认（Q-SV-2026-033）。正式封套字段位置/乱序窗口/错误映射已确认（Q-PH/SV-2026-039，2026-08-28）；后台终止态、超时数值最终验证、Connector 共同验收和正式封套 Schema 仍待完成；Relay 限流/容量已有 ECS E3 证据（2026-08-27）。
- 两端技术选型索引更新为已批准；未冻结的 Noise、容量和契约细节仍保持 pending。
- 建立跨端公开契约治理规范，约束真源、版本、同步、时间盒和新会话交接。

## 当前进行中

- G1 已确认的产品语义和技术参数已同步；当前只复核尚未进入公开契约的字段、失败语义和 Connector 共同验收项。D4 的静态密钥绑定、P0 rekey、会话序号、握手错误映射、初始超时、前台退避、后台生命周期、网络恢复触发、去抖、网络类型切换、重连尝试、Relay pairing 级限流边界、`60 秒/5 次` 初始参数、全局保护边界及 `100` 并发/`60 秒 300 次` Spike 初始基线已关闭。
- Phone Noise Spike：Phone 侧已完成本机 macOS Swift/Rust snow 固定向量、transport、负向检查和 iOS 17 device/simulator target 编译；Connector 共同验收与真机生命周期验证待完成。Phone 证据见其 `harness/evidence/2026-08-24/SPK-PH-2026-001-summary.md`。
- Server Relay Spike：本机合成门禁与目标 ECS 容量、背压、消息大小、pairing 级限流、重启和 reload 演练均已完成（2026-08-27，E3）；证据见 `harness/evidence/2026-08-24/SPK-SV-2026-001-summary.md` 与 `harness/evidence/2026-08-27/SPK-SV-2026-001-ecs-summary.md`。
- `harness/契约/integration-profile.yaml` 尚未进入 approved。
- 本工作批次两端契约 SHA-256：`1807d8c9b86149f6d80ee38a29539b08e31ae17a5491b604d7656b39e8230998`（逐字节一致）。

## 下一项待确认决策

- Connector 文档待确认清单 #1 已关闭:Owner=Zhu3xx(Q-SV-2026-038,2026-08-27);#2 固定向量共同签收改由 Zhu3xx 在 Connector 侧实现/验收时执行。
- 下一问:清单 #5 桌面首发兼容矩阵(系统版本/打包格式);清单 #3/#4 已于 2026-08-28 关闭(Q-PH/SV-2026-039/040),ECS E3 Spike 已于 2026-08-27 完成。

## 阻塞项

- Relay 全局保护最终参数、容量、Caddy/应用层职责边界和测试向量未由 Phone/Connector 双方冻结；`100` 并发/`60 秒 300 次`仅为已确认的 Spike 初始基线。静态公钥绑定、P0 rekey、会话序号、错误映射、初始超时、前台退避、后台生命周期、网络恢复触发、去抖、网络类型切换、重连尝试和限流边界已确认；实现、Connector 共同验收和正式公开封套证据仍缺失。
- Relay 阿里云 ECS 容量、背压、消息大小和 Caddy reload 演练已于 2026-08-27 完成（E3，见 `harness/evidence/2026-08-27/SPK-SV-2026-001-ecs-summary.md`）；公网 TLS/域名路径、真实 Noise+Schema+SQLite 链路容量和断网注入仍未覆盖。
- Connector Service 的正式公开契约尚未建立;《Connector 职责边界与公开接口范围》已起草(v0.1 草案),待逐项 Q&A 确认后进入 profile 各 section。

## 下一步

1. 确认 Connector Owner(文档待确认清单 #1);随后逐项确认公开面文档其余 8 项。
2. 让 Connector Owner 共同签收 Phone 固定向量和失败语义。
3. ECS Relay Spike 已于 2026-08-27 完成（E3）；剩余 G2 项为手机真机 WSS 生命周期与 Keychain 边界、Noise 固定向量 Connector 共同签收。
4. 契约 approved 后再丰满 Harness，禁止提前写生产实现。

## 时间盒

- 单个需求/决策问题：一次工作会话，建议 60–90 分钟内完成记录或明确阻塞。
- 单个高风险 Spike：最多 1 个工作日，超时必须更新阻塞原因。
- 首个 Harness 纵向切片：契约冻结后 1 个工作日。

## 人类可读交接要求

- 会话开始：读取 `AGENTS.md`、本文件、`文档/交流记录/README.md`、`文档/交流记录/项目驾驶舱.md` 和相关证据。
- 会话汇报：先给驾驶舱摘要，再给测试站证据；保留专业名词、English/缩写、字段和参数，每次最多引入 1–3 个新术语。
- 会话结束：更新驾驶舱、主交流记录、术语学习卡（若有新词）和证据索引（若本次有对应变化）。
- 进度状态：必须区分 `已证实`、`待证据`、`待决策`、`待开发`；不得只写“进行中”或百分比。
