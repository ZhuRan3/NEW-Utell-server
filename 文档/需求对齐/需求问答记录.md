# 需求问答记录

> 新项目从零建立。本文件不复制旧项目的问答、决策或结论。

## 使用说明

每次新需求对齐都新增一个编号，保留用户原文、问题、全部选项、用户回答、证据、结论、影响文件、未决事项和回滚条件。未确认内容只能标为“待确认”。

## 新项目记录模板

### Q-SV-YYYY-NNN：标题

- 日期：
- 关联需求：`REQ-SV-YYYY-NNN`
- 用户原文：
- 需要确认的问题：
- 选项与取舍：
  - A：
  - B：
- 用户回答原文：
- 证据：
- 结论：
- 影响文件：
- 未决事项：
- 回滚条件：
- 状态：待确认/用户已确认/暂定建议/已否决/已变更

---

## 首轮对齐记录（2026-08-22）

### Q-SV-2026-001：云端 Relay 技术选型在新项目中如何处理

- 日期：2026-08-22
- 关联需求：项目从 0 重建的总体要求
- 用户原文：「旧项目中我们只保留方法论，规范的意识和工作链路开发方式，所有的决策，文件，文档都不保留。决策、文件、文档全部都在新项目中新建，都不复用。」
- 需要确认的问题：Relay 语言/运行时、边缘代理与元数据存储（旧项目曾决策 Go + Caddy + SQLite）在新项目中直接沿用、标注待确认，还是重新开放选型？
- 选项与取舍：
  - A：走 Q&A 正式确认旧方向（Go + Caddy + SQLite）。记录理由后直接写入基线，省去重复调研；但属于直接继承旧决策，与"决策不保留"原则有张力。
  - B：先标注"暂定建议/待确认"。等技术选型决策包（候选对比 + 证据）完成后再冻结；中间期间文档不含既定技术栈。
  - C：重新开放选型。重新评估候选（如 Go、Rust、Node/TypeScript 等），先产出选型决策包，再由用户决定。
- 用户回答原文：选择 C「重新开放选型」。
- 证据：本仓库 PRD（不绑定具体技术栈）；旧项目决策已随旧仓库删除，不作为输入。
- 结论：云端 Relay 技术选型重新开放。在新决策包完成并经用户批准前，本仓库所有文档不得把任何语言、代理或存储写成既定结论；仅允许标注"待确认"。选型决策包骨架建于 `文档/技术选型/TS-SV-2026-001-Relay语言与运行形态.md`。
- 影响文件：`开发规则规范/03-架构分层与仓库边界.md`、`文档/架构与契约/架构基线.md`、新建 `文档/技术选型/`。
- 未决事项：候选清单、硬约束（单 active Connector、最小元数据、无法解密）、容量输入与 Spike 范围，待决策包调研补齐。
- 回滚条件：若决策包证据显示重新选型成本不可接受，可重新打开本条并改为直接确认某候选。
- 状态：用户已确认

### Q-SV-2026-002：两端开发规则规范文件的同步策略

- 日期：2026-08-22
- 关联需求：「两个文件夹下的规范规则和 harness 开发架构文件夹分配路径都是保持一致的」
- 用户原文：「两个文件夹下的规范规则和 harness 开发架构文件夹分配路径都是保持一致的。」
- 需要确认的问题：手机端与云端的开发规则规范文件，是逐字一致还是允许各自演化？
- 选项与取舍：
  - A：逐字一致 + 角色段落。除明确标记的角色差异段落（如测试分层、ID 前缀、场景清单）外，规范文件逐字一致；02 号文等以内容更完整的版本为基准补齐另一端。
  - B：允许各自演化。只保证方法论一致，细节按角色自由发展；长期会产生规范漂移。
- 用户回答原文：选择 A「逐字一致+角色段落」。
- 证据：用户总体要求两端路径一致、便于快速查阅。
- 结论：六份规范文件共同部分逐字一致；角色相关内容集中在显式标记的「本端角色」段落。修改任一端共同部分时必须同步另一端。
- 影响文件：`开发规则规范/01~06` 全部文件。
- 未决事项：无。
- 回滚条件：若同步维护成本实际过高，可重新打开本条改为"仅方法论一致"。
- 状态：用户已确认

### Q-SV-2026-003：GitHub 远程仓库可见性

- 日期：2026-08-22
- 关联需求：「新建两个 git 仓库，作为开发云端备份」
- 用户原文：「一致开发完成之后，请你新建两个 git 仓库，作为开发云端备份。」
- 需要确认的问题：NEW-Utell-phone 与 NEW-Utell-server 两个新仓库设为公开还是私有？（旧 NEW-Utell 仓库为 public，新仓库含未发布产品 PRD。）
- 选项与取舍：
  - A：Private 私有。未发布 PRD 与架构细节不公开。
  - B：Public 公开。任何人可见。
- 用户回答原文：选择 B「Public 公开」。
- 证据：用户明确选择。
- 结论：两个仓库均以 public 创建。仓库内不得提交密钥、凭据、真实用户数据（已由 .gitignore 与安全规范兜底）。
- 影响文件：远程仓库设置；无代码文件。
- 未决事项：旧仓库 ZhuRan3/NEW-Utell 的删除需要 gh delete_repo 授权后执行。
- 回滚条件：随时可在 GitHub 设置中改为私有。
- 状态：用户已确认


### Q-SV-2026-004：所有选型与需求回到待确认并逐项重新确认

- 日期：2026-08-23
- 关联需求：项目从 0 重建的总体要求
- 用户原文：「我需要所有的选型及其需求都回到待确认状态，一个一个跟我重新确认和功能的对应。」
- 需要确认的问题：已建决策包与架构基线中，哪些内容受旧项目分析影响、如何处理？（我方如实申报：硬约束来自新 PRD、证据为 2026-08-22 实时调研；但候选框架与 AI 建议方向与旧项目结论存在连续性，用户知悉后作出本条决定。）
- 选项与取舍：
  - A：全面重开——所有选型与需求回到待确认，逐项重新确认。
  - B：部分重开——仅重开用户指定的结论。
  - C：接受现状——证据与约束来源可靠，审批时把关即可。
- 用户回答原文：选择 A（即本条用户原文）。
- 证据：本仓库 PRD；两端仓库文档来源审计对话（2026-08-22 会话）。
- 结论：建立 `文档/需求对齐/需求确认台账.md`，全部条目初始化为"待确认"；架构基线与决策包加"逐项确认中"状态头；决策包中的候选框架与 AI 建议降级为调研参考材料；逐项确认完成前不开始任何实现。
- 影响文件：`文档/需求对齐/需求确认台账.md`（新建）、`文档/架构与契约/架构基线.md`、`文档/技术选型/TS-SV-2026-001-Relay语言与运行形态.md`。
- 未决事项：台账全部条目的逐项确认。
- 回滚条件：无（本条即回滚动作本身）。
- 状态：用户已确认

### Q-SV-2026-005：A1 四层链路骨架确认 + 对齐流程约定

- 日期：2026-08-23
- 关联需求：需求确认台账 A1
- 用户原文：「确认。但我需要声明的一点是，后续对齐需求和决策的时候，请你先行复核 PRD 文档（项目总体背景愿景目标/PRD：Utell（油条）第一代智能日志 MVP.md）当中提到的部分，一切以这份文档为准，其中没提到的部分继续向我提问。」
- 需要确认的问题：是否确认「手机 ↔ Relay ↔ PC Connector ↔ Utell Pi」四层链路作为第一代架构骨架？
- 选项与取舍：
  - A：确认。四层链路为架构基石，后续条目在此基础上确认。
  - B：调整后确认。某些环节（直连、Relay 职责、Pi 角色）需修改。
  - C：否决重议。重新讨论架构形态。
- 用户回答原文：选择 A「确认」（附上述流程约定）。
- 证据：PRD 0.1/0.2（产品定义与核心架构、数据边界）。
- 结论：A1 确认。四层链路为第一代架构骨架。**附带流程约定（适用于全部后续对齐）：逐项确认时先复核 PRD；PRD 已明确的部分以 PRD 为准直接登记，不重复提问；PRD 未提及或标注"待确认"的部分才向用户提问。**
- 影响文件：`文档/需求对齐/需求确认台账.md`（A1 状态）、两端架构基线。
- 未决事项：无。
- 回滚条件：若产品形态变化（如去 Relay 直连），重新打开本条。
- 状态：用户已确认

### Q-SV-2026-006：A2–A6 依据 PRD 登记（流程约定首次执行）

- 日期：2026-08-23
- 关联需求：需求确认台账 A2–A6
- 用户原文：「后续对齐需求和决策的时候，请你先行复核 PRD 文档……一切以这份文档为准，其中没提到的部分继续向我提问。」（Q-SV-2026-005）
- 需要确认的问题：A2–A6 是否已被 PRD 明确覆盖，可直接登记？
- PRD 复核结果（逐条引用）：
  - A2 数据权威在 Connector、手机仅存内存禁持久化：PRD 0.2「个人电脑是唯一业务数据权威方，手机卡片是其状态投影」；2.3「手机临时草稿和本次会话的卡片仅存内存」；7.3「手机禁止持久化业务原文、待发送队列、自动重发任务和业务卡片缓存」。→ PRD 已明确。
  - A3 E2EE 端点终止、Relay 只见密文：PRD 0.2「Relay 仅路由密文」；7.3「私钥只在端点安全存储」。→ PRD 已明确。
  - A4 一用户一电脑一手机：PRD 2.3「P0 限定一名用户配对一台个人电脑和一台手机」。→ PRD 已明确。
  - A5 Relay 无离线队列、未投递即丢弃：PRD 7.2「不建立离线投递队列」；FR-008/AC-019「Relay 不将其持久化为离线队列，返回路由不可投递」。→ PRD 已明确。
  - A6 Relay ACK ≠ 权威状态：PRD 2.1「Relay ACK 不得冒充完成」；FR-003/AC-006。→ PRD 已明确。
- 结论：A2–A6 全部以 PRD 为依据登记为"用户已确认（PRD 依据）"，不重复提问；用户可在任一时刻推翻任一条目。
- 影响文件：`文档/需求对齐/需求确认台账.md`。
- 未决事项：无。
- 回滚条件：用户明示修改任一登记条目时，按变更流程重新打开。
- 状态：用户已确认

### Q-SV-2026-007：手机端 B2–B8 依据 PRD 登记

- 日期：2026-08-23
- 关联需求：需求确认台账 B2–B8
- 用户原文：同流程约定（Q-SV-2026-005）：PRD 已明确的部分以 PRD 为准直接登记。
- PRD 复核结果：B2（5.2/FR-002/FR-003）、B3（5.3/FR-002）、B4（FR-004/5.4）、B5（FR-005/5.4）、B6（5.5）、B7（FR-007）、B8（2.2）均有完整定义与验收标准，无歧义、无"待确认"标注。
- 结论：B2–B8 登记为"用户已确认（PRD 依据）"。B1 配对流程主体已由 PRD FR-001 覆盖，但 pairing token 有效期为 PRD 建议值（5 分钟、一次性，待安全评审确认），B1 保持待确认直至该参数确认。
- 影响文件：`文档/需求对齐/需求确认台账.md`。
- 状态：用户已确认

### Q-SV-2026-008：B1 配对 token 参数确认

- 日期：2026-08-23
- 关联需求：需求确认台账 B1（FR-001 配对）
- 用户原文：选择「接受 5 分钟一次性」。
- 需要确认的问题：PRD 建议 pairing token「有效期 5 分钟、仅可使用一次，待安全评审确认」，是否接受？
- 选项与取舍：A：接受 5 分钟一次性（安全面最小，推荐）；B：调整参数（如 15 分钟或可重试）；C：保持待安全评审。
- 用户回答原文：选择 A。
- 证据：PRD FR-001、AC-001/AC-002。
- 结论：配对 token 有效期 5 分钟、一次性使用，冻结为 P0 参数；写入契约时必须包含过期与已使用拒绝路径。B1 全部确认完毕。
- 影响文件：`文档/需求对齐/需求确认台账.md`（B1）；后续 Relay Transport 契约。
- 未决事项：无。
- 回滚条件：安全评审提出更强/更弱要求时按变更流程调整。
- 状态：用户已确认

### Q-SV-2026-009：云端 C1/C3/C5/C6/C7 依据 PRD 登记

- 日期：2026-08-23
- 关联需求：需求确认台账 C1/C3/C5/C6/C7
- 用户原文：同流程约定（Q-SV-2026-005）。
- PRD 复核结果：C1（FR-008 全条）、C3（7.4 错误码表）、C5（FR-001/AC-018、2.3 单主限定）、C6（FR-008/AC-020）、C7（7.3 发布门禁、9.2 清单）均有明确定义。
- 结论：上述条目登记为"用户已确认（PRD 依据）"。C2（审计 30 天留存）与 C4（健康上报 10s / READY 判定 30s）为 PRD 待确认参数，保持待确认。
- 影响文件：`文档/需求对齐/需求确认台账.md`。
- 状态：用户已确认

### Q-SV-2026-010：时序参数确认（健康上报 10s / READY 判定 30s）

- 日期：2026-08-23
- 关联需求：需求确认台账 C4、B2（联动）
- 用户原文：选择「接受 10s/30s」。
- 需要确认的问题：PRD 建议「Connector 每 10 秒上报健康状态；手机以 30 秒为健康新鲜度上限判定 READY」（均为建议值/待确认），是否接受？
- 选项与取舍：A：接受 10s/30s（保守、简单，推荐）；B：调整（如 5s/15s，发现断点更快但流量翻倍）；C：保持待定，Spike 后冻结。
- 用户回答原文：选择 A。
- 证据：PRD 5.2、FR-003、AC-007。
- 结论：Connector 健康上报周期 10 秒、手机健康新鲜度上限 30 秒，冻结为 P0 参数；变更时必须同步测试用例与状态文案（PRD 5.2 要求）。
- 影响文件：`文档/需求对齐/需求确认台账.md`（C4）；后续 Relay Transport/Connector Service 契约。
- 未决事项：无。
- 回滚条件：Spike 或真机证据显示该节奏不满足体验/容量时重新打开。
- 状态：用户已确认

### Q-SV-2026-011：C2 审计元数据留存期限确认（14 天，变更 PRD 建议值）

- 日期：2026-08-23
- 关联需求：需求确认台账 C2（FR-008、PRD 7.2）
- 用户原文：「14 天」。
- 需要确认的问题：PRD 建议 Relay 脱敏审计元数据「最长保留 30 天，到期自动删除，待确认」，是否接受？
- 选项与取舍：A：接受 30 天（与 PRD 一致）；B：调整天数（如 7 天更隐私友好 / 90 天更便于排障）；C：不保留审计。
- 用户回答原文：自定义回答「14 天」。
- 证据：用户明确选择；隐私最小化取向。
- 结论：Relay 脱敏审计元数据（pairing 哈希、时间戳、消息大小区间、投递结果、错误类别）最长保留 **14 天**，到期自动删除。**这是对 PRD 建议值的变更，PRD 已同步更新至 v0.3（7.2 节、8.3 隐私合规行、9.3 变更记录）。**
- 影响文件：两端 PRD（v0.3）、`文档/需求对齐/需求确认台账.md`（C2）、后续 Relay 实现的留存清理任务。
- 未决事项：隐私合规其余项（诊断导出范围）随 FR-007 实施时再核。
- 回滚条件：排障实践中证据不足时可重新打开调整。
- 状态：用户已确认

### Q-SV-2026-012：D1 首发手机平台范围确认（仅 iOS）

- 日期：2026-08-23
- 关联需求：需求确认台账 D1（TS-PH-2026-001 子问题 a）
- 用户原文：选择「首发仅 iOS」。
- 需要确认的问题：第一代智能日志 MVP 的首发手机平台范围？
- 选项与取舍：A：首发仅 iOS（单 Owner 交付最快，Android 后置需重写）；B：iOS+Android 同期（倾向跨端方案，成本翻倍）；C：首发仅 Android。
- 用户回答原文：选择 A。
- 证据：决策包 TS-PH-2026-001（候选矩阵与证据，2026-08-22 实时核实）。
- 结论：第一代首发平台仅 iOS；Android 明确后置，不作为 P0 任何验收的输入。D2（UI 技术栈）在此前提下继续确认。
- 影响文件：`文档/需求对齐/需求确认台账.md`（D1）；TS-PH-2026-001。
- 未决事项：最低 iOS 版本（随 D2 冻结）。
- 回滚条件：若产品决定 Android 提前，重新打开本条与 D2。
- 状态：用户已确认

### Q-SV-2026-013：D2 手机技术栈与最低系统版本确认（SwiftUI + iOS 17）

- 日期：2026-08-23
- 关联需求：需求确认台账 D2（TS-PH-2026-001，前置 D1=Q-SV-2026-012 首发仅 iOS）
- 用户原文：D2 选择「SwiftUI 原生（推荐）」；D2b 选择「iOS 17+」。
- 需要确认的问题：首发仅 iOS 前提下的 UI 技术栈与最低系统版本？
- 选项与取舍（D2）：A：SwiftUI 原生（P0 能力全官方覆盖、本机已就绪、交付最快，推荐）；B：UIKit 原生（更成熟但模板代码多）；C：Flutter 单端（失去双端优势且值三迁移叠加期）。选项（D2b）：iOS 17+ / iOS 18+ / 跟随最新。
- 用户回答原文：D2 选 A；D2b 选 iOS 17+。
- 证据：TS-PH-2026-001 决策包（E1/E2 证据，2026-08-22 实时核实）。
- 结论：手机端 = iOS 原生 + SwiftUI，最低 iOS 17。已回写 TS-PH-2026-001 开发者决定（状态：开发者已批准）、phone 架构基线、AGENTS.md、README 与两端台账。
- 影响文件：上述文件；后续 Mobile Shell 骨架与 Spike。
- 未决事项：E2EE 密码套件（D4）；最低版本的 Spike 验证证据。
- 回滚条件：Android 提前或 Spike 发现阻塞性缺陷时重新打开。
- 状态：用户已确认

### Q-SV-2026-014：D3 Relay 技术栈与云服务商确认（Go + Caddy + SQLite + 阿里云）

- 日期：2026-08-23
- 关联需求：需求确认台账 D3（TS-SV-2026-001）
- 用户原文：D3a 选择「Go（推荐）」；D3b 选择「Caddy（推荐）」；D3c 选择「阿里云」。
- 需要确认的问题：Relay 服务语言/运行时、公网边缘代理、部署云服务商？
- 选项与取舍：D3a：Go（连接并发/静态单二进制/标准库 TLS，推荐）/ Rust（资源可预测但 Web 栈全 0.x）/ Node（开发快但无单二进制、事件循环阻塞风险）。D3b：Caddy（自动 HTTPS+原生 WSS 反代，reload 断连需规避，推荐）/ Nginx（成熟但证书自理）/ 无代理直连。D3c：阿里云（国内延迟低、有使用经验）/ 其他国内云 / 海外方案。
- 用户回答原文：D3a=Go；D3b=Caddy；D3c=阿里云。
- 证据：TS-SV-2026-001 决策包（E1/E2 证据，2026-08-22 实时核实）。
- 结论：Relay = Go 1.27+ + coder/websocket + modernc.org/sqlite（纯 Go 无 cgo 静态单二进制）+ Caddy v2.11+ + 阿里云单台 ECS。已回写 TS-SV-2026-001 开发者决定（开发者已批准）、server 架构基线、AGENTS.md、README 与两端台账。元数据存储随语言采用 modernc.org/sqlite（嵌入式 SQLite）。
- 影响文件：上述文件；后续 Relay 生产骨架、Relay Transport 契约与 Spike。
- 未决事项：容量/背压 Spike（E3）；生产身份 Provider（D5）；E2EE 测试向量（D4）。
- 回滚条件：Spike 不达标时保持契约重审 Rust/Node；备案/区域问题出现时重审服务商。
- 状态：用户已确认

### Q-SV-2026-015：E2 发布与回滚策略依据 PRD 登记

- 日期：2026-08-23
- 关联需求：需求确认台账 E2
- 用户原文：同流程约定（Q-SV-2026-005）。
- PRD 复核结果：PRD 9.1 已明确发布顺序（先 Connector+Relay 端到端验证，再发手机端）、灰度（首批内部测试用户、每阶段观察 ≥7 天且无 P0 安全事故）、回滚（手机停止分发可回滚；Relay 回滚至兼容版本；Connector 仅备份成功后可回滚；任何回滚不删权威数据）与兼容矩阵要求。无歧义。
- 结论：E2 登记为"用户已确认（PRD 依据）"。
- 影响文件：`文档/需求对齐/需求确认台账.md`（E2）。
- 状态：用户已确认

### Q-SV-2026-016：D5 第一代不设账号体系

- 日期：2026-08-23
- 关联需求：需求确认台账 D5（PRD 未提及，属补充决策）
- 用户原文：选择「不需要账号体系」。
- 需要确认的问题：PRD 未提及账号体系，第一代是否需要用户账号？
- 选项与取舍：A：不需要账号体系——设备配对即身份，手机与电脑各持密钥对，Relay 只认 pairing（最简单、隐私面最小、与 PRD 一致，推荐）；B：需要轻量账号（为未来多设备预留，但超出现 PRD）；C：其他身份方案。
- 用户回答原文：选择 A。
- 证据：PRD FR-001 配对流程（扫码+token+公钥指纹即可闭环）；A4 一用户一机一电脑。
- 结论：第一代无注册/登录/账号 Provider；身份 = pairing + 端点密钥对。若未来引入多设备/换机迁移，再开新 Q&A 评估账号体系。已同步 PRD v0.4 变更记录。
- 影响文件：两端 PRD（v0.4）、`文档/需求对齐/需求确认台账.md`（D5）、server 架构基线。
- 未决事项：无。
- 回滚条件：多设备或跨机迁移需求出现时重新打开。
- 状态：用户已确认

### Q-SV-2026-017：D4 E2EE 方向确认为 Noise，先 Spike 再双方冻结

- 日期：2026-08-23
- 关联需求：需求确认台账 D4
- 用户原文：选择「Noise 方向，先 Spike」。
- 需要确认的问题：E2EE 协议套件方向？（跨端契约，需 Painfox 共同接受测试向量）
- 选项与取舍：A：Noise 方向先 Spike——主流框架、Rust 侧 snow 维护活跃，但 iOS 无主流 Noise 库，需先验证 Swift 可行性；B：libsodium 系自定握手（成熟库但偏离标准框架）；C：留待双方共定。
- 用户回答原文：选择 A。
- 证据：2026-08-22 调研（Rust snow 0.10.0 活跃；iOS Noise 生态薄弱）。
- 结论：E2EE 采用 Noise 协议族方向。冻结前必须完成：SPK 级 Spike（iOS Swift 侧 Noise 握手与 transport 可行性、与 Connector 候选实现的互操作、固定测试向量）；随后与 Painfox 共同接受测试向量并写入 integration-profile。禁止自创密码原语。
- 影响文件：`文档/需求对齐/需求确认台账.md`（D4）、server 架构基线；后续 harness/契约/ 测试向量目录。
- 未决事项：具体 Noise pattern、握手角色、0-RTT 取舍、rekey 策略（Spike 后双方共定）。
- 回滚条件：iOS 侧 Spike 失败时改评 libsodium 系方案。
- 状态：方向已确认（细节待 Spike + 双方共定）

### Q-SV-2026-018：E1 P0 成功指标阈值确认

- 日期：2026-08-23
- 关联需求：需求确认台账 E1（PRD 1.3）
- 用户原文：选择「接受 PRD 建议值」。
- 需要确认的问题：P0 成功指标阈值是否接受 PRD 建议值？
- 选项与取舍：A：接受（配对 ≥90%、有效提交接收率 ≥99%、接收 P95 ≤5s、投影 P95 ≤15s、解析可用率 ≥95%、原文残留=0）；B：调整部分指标；C：保持待定至试运行。
- 用户回答原文：选择 A。
- 证据：PRD 1.3 指标表。
- 结论：P0 指标阈值冻结为 PRD 建议值，PRD 1.3 标注更新为已确认（v0.4）；试运行期间实测复核。
- 影响文件：两端 PRD（v0.4）、`文档/需求对齐/需求确认台账.md`（E1）。
- 未决事项：无。
- 回滚条件：试运行数据显示阈值不切实际时按变更流程调整。
- 状态：用户已确认

### Q-SV-2026-019：原文追溯保留在 P0 的在线读取语义

- 日期：2026-08-23
- 关联需求：需求确认台账 A7（跨端共同，PRD FR-004、5.1、7.1）
- 用户原文：「确认」
- 需要确认的问题：卡片“原文追溯”是否保留在 P0，以及在手机不得持久化业务原文的约束下如何实现？
- 选项与取舍：A：保留为在线按需读取——用户打开入口后由手机向 Connector 请求，原文只在当前会话内存展示，不进入 Projection 持久缓存且失败时不自动重试（推荐）；B：从 P0 移除原文追溯，卡片只展示 Projection 字段。
- 用户回答原文：确认 A。
- 证据：用户确认本条语义；PRD 2.3、5.1、7.1 已规定手机不得持久化业务原文。
- 结论：原文追溯保留在 P0。Relay 只转发该端到端加密的在线请求与响应，不建立队列、不落盘；具体请求/响应字段、权限校验和审计字段随 Connector Service 公开契约冻结。
- 影响文件：两端 PRD（v0.5）、两端需求确认台账 A7、server 架构基线；后续 Relay Transport/Connector Service 契约与 Harness 场景。
- 未决事项：具体请求/响应字段、权限校验和审计字段随 Connector Service 公开契约冻结。
- 回滚条件：安全评审或 Connector 契约无法满足无持久化约束时，重新评估是否移出 P0。
- 状态：用户已确认

### Q-SV-2026-020：原文追溯请求采用 event_id-only

- 日期：2026-08-23
- 关联需求：需求确认台账 A7、PRD FR-004/5.1/7.1
- 用户原文：「确认」
- 需要确认的问题：原文追溯请求是否只携带 `event_id`，由 Connector 内部解析对应 Capture，还是向手机公开 `capture_id`/`log_entry_id`？
- 选项与取舍：A：仅携带 `event_id`，Connector 内部完成关联（减少跨端耦合和内部标识暴露，推荐）；B：公开 `capture_id` 或 `log_entry_id`，便于直接查询但扩大公开契约和内部数据耦合。
- 用户回答原文：确认 A。
- 证据：用户确认本条字段边界；PRD 7.1 规定 Capture 与 LogEntry 只在 Connector 权威库保存。
- 结论：原文追溯公开请求只携带 `event_id`。Connector 在内部解析对应 Capture；手机和 Relay 不暴露、不依赖 `capture_id` 或 `log_entry_id`。Relay 只转发端到端密文，响应仍只在手机当前会话内存展示，不持久化、不自动重试。
- 影响文件：两端需求确认台账 A7、两端架构基线、两端 `harness/契约/integration-profile.yaml`；后续 Relay Transport/Connector Service Schema 和 Harness 场景。
- 未决事项：响应字段、授权校验和审计元数据随 Connector Service 公开契约冻结。
- 回滚条件：Connector 无法在不暴露内部标识的前提下完成稳定关联时，重新评估公开字段，但不得自行扩大字段。
- 状态：用户已确认

### Q-SV-2026-021：Noise 首发 Pattern 确认

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD 7.3
- 用户原文：「确认」
- 需要确认的问题：首发 E2EE Noise Pattern 采用哪一组固定组合？
- 选项与取舍：A：`Noise_XX_25519_ChaChaPoly_SHA256`——双方在握手中互相确认静态身份，与扫码配对和一次性 token 兼容，但需要完整握手往返（推荐）；B：`Noise_IK_25519_ChaChaPoly_SHA256`——握手更快，但要求发起方在握手前可靠持有并绑定 Connector 静态公钥；C：先不选 Pattern，继续保持整个 E2EE 方向待定。
- 用户回答原文：确认 A。
- 证据：PRD 7.3 要求端点 E2EE、成熟密码库和固定测试向量；Q-SV-2026-017 已确认 Noise 方向先 Spike。
- 结论：首发 Pattern 选择 `Noise_XX_25519_ChaChaPoly_SHA256`。这只关闭 Pattern 选择，不代表互操作或安全验证完成；Relay 不实现 Noise，只转发密文。必须完成 iOS 与 Rust snow 的握手/transport 互操作、固定测试向量、rekey、重放/篡改拒绝证据，并由双方接受后才可将 `integration-profile` 标记为 `approved`。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Phone Noise Spike 和后续 Relay Transport 契约。
- 未决事项：握手角色映射、静态公钥来源与绑定、rekey 周期、transport nonce/序号策略、向量格式和双方验收签字。
- 回滚条件：iOS 17 目标无法使用维护中的非自研实现，或与 Rust snow 互操作失败时，按 Q-SV-2026-017 回退评估 libsodium 系方案。
- 状态：用户已确认（Pattern）；Phone/Rust snow Spike 与固定向量已完成，Connector 共同验收待完成

### Q-SV-2026-022：Noise 握手角色映射

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD 4.0/7.3
- 用户原文：「确认」
- 需要确认的问题：`Noise_XX_25519_ChaChaPoly_SHA256` 中 Phone 与 Connector 的握手角色如何映射？
- 选项与取舍：A：Phone = Initiator、Connector = Responder——手机建立 WSS 后主动开始应用层握手，与配对、READY 和恢复连接流程一致（推荐）；B：Connector = Initiator、Phone = Responder——需要 Connector 主动调度握手；C：按连接方向动态切换角色——实现和测试复杂度更高。
- 用户回答原文：确认 A。
- 证据：PRD 4.0 规定手机经 Relay 与电脑 Connector 建立链路；PRD FR-003 规定手机恢复连接后重新健康检查和同步。
- 结论：Phone 固定为 Noise Initiator，Connector 固定为 Noise Responder；Relay 不参与握手、不持有 E2EE 密钥，只转发 WSS 密文。该角色映射适用于初次配对后的会话建立和后续恢复连接。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Phone Noise Spike 和后续 Relay Transport 契约。
- 未决事项：静态密钥来源与 pairing 绑定、rekey 周期、transport nonce/序号、握手超时和错误映射、固定向量。
- 回滚条件：实现库或 Connector 生命周期无法稳定支持固定角色时，重新评估角色映射，但不得在运行时无记录地动态切换。
- 状态：用户已确认（Pattern/角色）；Phone/Rust snow Spike 与固定向量已完成，Connector 共同验收待完成

### Q-SV-2026-023：Noise 静态密钥与 pairing 绑定

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD 7.3、FR-001
- 用户原文：「确认」
- 需要确认的问题：Noise 静态密钥如何生成、保存并与扫码配对身份绑定？
- 选项与取舍：A：Connector 首次启动生成并持久化静态密钥；配对二维码包含 Connector 静态公钥指纹和一次性 token；Phone 扫码后生成并保存自己的静态密钥；双方将静态公钥指纹与 `pairing_id` 绑定，并在握手后校验匹配（推荐）；B：每次会话临时生成密钥，降低持久化管理成本但无法稳定识别端点；C：由 Relay 代持或生成端点私钥，简化配对但破坏端到端密钥边界。
- 用户回答原文：确认 A。
- 证据：PRD FR-001 要求扫码、一次性 token 和公钥指纹配对；PRD 7.3 要求私钥只在端点安全存储，Relay 不持有 E2EE 密钥。
- 结论：Connector 首次启动生成并持久化 Noise 静态密钥；配对二维码携带 Connector 静态公钥指纹与一次性 token；Phone 扫码后生成并保存 Phone 静态密钥。双方把静态公钥指纹和 `pairing_id` 绑定，Noise 握手完成后校验握手内静态公钥与已确认 pairing 匹配。Relay 只保存公钥和 pairing 元数据，不保存任何私钥。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Phone/Connector Noise Spike 与后续配对契约。
- 未决事项：私钥存储 API 的具体实现、rekey 周期、transport nonce/序号、握手超时和错误映射、固定向量及双方验收签字。
- 回滚条件：端点安全存储或握手内公钥校验无法在 iOS 17 与 Connector 候选实现中稳定完成时，重新评估绑定流程，但不得让 Relay 代持私钥。
- 状态：用户已确认（绑定方案）；实现/互操作/向量待完成

### Q-SV-2026-024：Noise transport rekey 触发策略

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD 7.3
- 用户原文：「a」
- 需要确认的问题：首发 P0 的 Noise transport 是否需要会话内 rekey，以及采用哪种触发策略？
- 选项与取舍：A：P0 只在会话重建时重新执行完整 Noise 握手；不增加自定义 rekey 控制帧，降低 Phone/Connector 状态机和互操作复杂度（推荐，前提是单会话时长与消息量受限）；B：按固定时间窗口触发会话内 rekey，需要双方新增控制消息、超时和失败语义；C：按消息数或密钥使用量阈值触发会话内 rekey，密码学边界更明确，但需要完整计数、并发和向量证据。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-021/022/023 已确认 Pattern、角色和静态密钥绑定；PRD 7.3 要求成熟密码库和固定测试向量，但未规定 rekey 触发方式。
- 结论：首发 P0 不增加会话内自定义 rekey 控制帧；仅在会话重建时重新执行完整 Noise 握手。该决策降低 Phone/Connector 状态机和互操作复杂度，但单会话时长与消息量限制、握手失败恢复和固定向量仍需 Spike 形成证据。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Noise Spike 与 transport 契约。
- 未决事项：单会话时长与消息量限制、transport nonce/序号、握手超时和错误映射、双方测试向量和失败恢复语义。
- 回滚条件：Spike 或互操作证据无法支持所选策略时，按本条重新开放，不得静默切换。
- 状态：用户已确认（rekey 策略）；实现/阈值/向量待完成

### Q-SV-2026-025：Noise transport nonce/序号策略

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD 7.3
- 用户原文：「a」
- 需要确认的问题：Noise transport 消息是否增加应用层方向序号，以及序号在重连和乱序/重放场景下如何处理？
- 选项与取舍：A：每个 Noise 会话按方向维护单调递增 `uint64` 序号，序号随密文封套传输；接收端拒绝重复或回退序号，重连完成新握手后从初始值重新开始（推荐，便于显式诊断和故障证据）；B：只依赖 Noise 内部 nonce，不增加公开序号，契约更小但难以区分应用层乱序、重放和重复投递；C：序号跨会话持久化并与 pairing 绑定，防重放边界更强但引入端点持久化状态和迁移复杂度。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-021/022/023/024 已确认 Pattern、角色、静态密钥绑定和重连 rekey；PRD 7.3 要求防重放和固定测试向量，但未规定应用层序号格式。
- 结论：每个 Noise 会话按方向维护单调递增的 `uint64` 应用层序号，序号随密文封套传输；接收端拒绝重复或回退序号。重连完成新的 Noise 握手后，双方序号从初始值重新开始。该序号用于显式诊断乱序、重放和重复投递；Relay 只转发封套，不解释业务密文。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Relay Transport Schema、Noise Spike 与 Harness 场景。
- 未决事项：序号在密文封套中的正式字段位置、是否允许有限乱序窗口、重放/回退错误映射和固定向量。
- 回滚条件：Spike 或互操作证据无法支持所选策略时，按本条重新开放，不得静默切换。
- 状态：用户已确认（序号策略）；字段/乱序窗口/向量待完成

### Q-SV-2026-026：Noise 握手超时与错误映射

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD 4.0/7.3、FR-003
- 用户原文：「a」
- 需要确认的问题：Noise 握手的超时边界和对 Phone 可见的错误映射如何定义？
- 选项与取舍：A：固定握手总超时，并将所有握手失败统一映射为脱敏的 `HANDSHAKE_FAILED`；底层原因只进入端点本地诊断，避免暴露协议细节（推荐）；B：按阶段暴露不同握手错误码，诊断更细但扩大跨端契约和信息泄露面；C：无限等待或由底层库自行决定，契约简单但可能造成连接状态悬挂。
- 用户回答原文：选择 A。
- 证据：PRD FR-003 要求恢复连接后重新健康检查；治理规范要求错误码和失败语义进入公开契约后才可实现；Q-SV-2026-024/025 已确认重连握手和序号重置。
- 结论：Noise 握手采用固定总超时；所有握手失败对 Phone 统一映射为脱敏错误 `HANDSHAKE_FAILED`，底层失败原因只进入 Phone/Connector 端点本地诊断，不进入 Relay 或跨端公开错误细节。具体超时数值和重连退避由 Spike 确定。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Relay Transport Schema、Noise Spike 与 Harness 场景。
- 未决事项：具体超时数值、端点本地诊断字段格式和重连退避行为。
- 回滚条件：Spike 证明固定超时无法覆盖目标网络环境时，重新开放本条，不得静默延长等待。
- 状态：用户已确认（策略）；具体数值/诊断格式/退避待 Spike

### Q-SV-2026-027：Noise 握手总超时数值

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD FR-003
- 用户原文：「a」
- 需要确认的问题：首发 P0 的固定 Noise 握手总超时采用哪一个初始数值？
- 选项与取舍：A：10 秒，兼顾移动网络重试体验与避免连接状态悬挂（推荐，Spike 后可按证据调整）；B：30 秒，给弱网更多完成握手的机会但会延长失败反馈；C：先由 Spike 在目标网络矩阵中测量，再冻结数值。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-026 已确认固定总超时和统一脱敏错误映射；PRD FR-003 要求恢复连接后重新健康检查，但未规定具体秒数。
- 结论：首发 P0 Noise 握手总超时初始值设为 `10 秒`，兼顾移动网络重试体验与失败反馈速度。该数值需在目标网络矩阵 Spike 中验证；若 P95 或失败率不达标，按证据调整并重新记录变更。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Noise Spike 与 Harness 场景。
- 未决事项：目标网络矩阵证据、重连退避参数和最终是否保留 `10 秒`。
- 回滚条件：实测 P95 或失败率不达标时按证据重开本条。
- 状态：用户已确认（初始值）；Spike 验证待完成

### Q-SV-2026-028：Noise 握手失败后的重连退避

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD FR-003
- 用户原文：「a」
- 需要确认的问题：Noise 握手失败或达到 `HANDSHAKE_FAILED` 后，Phone 的自动重连退避策略如何定义？
- 选项与取舍：A：指数退避并设置上限，前台会话可立即触发一次重连，后续按 `1s/2s/4s/8s/16s` 递增并封顶；成功握手后清零（推荐，避免弱网下连接风暴）；B：固定间隔重连，行为简单但可能造成持续请求压力；C：握手失败后不自动重连，只允许用户手动触发，节省资源但恢复体验较差。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-026/027 已确认固定握手超时、统一错误和 `10 秒` 初始值；PRD FR-003 要求恢复连接后重新健康检查和同步，但未规定退避。
- 结论：Phone 在前台会话中对 Noise 握手失败执行自动重连：首次可立即触发一次，后续按 `1s/2s/4s/8s/16s` 指数退避并封顶；成功握手后清零退避计数。该策略用于避免弱网下连接风暴，具体前后台差异、网络状态触发和最大尝试次数仍需单独确认或 Spike。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Phone/Relay Transport 场景。
- 未决事项：后台行为、网络状态触发、最大尝试次数/封顶时长和 Relay 压力证据。
- 回滚条件：Spike 证明所选退避导致恢复率或 Relay 压力不达标时，重新开放本条。
- 状态：用户已确认（前台退避策略）；后台/上限/触发条件待完成

### Q-SV-2026-029：后台状态下的自动重连

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD FR-003、5.2
- 用户原文：「a」
- 需要确认的问题：Phone 进入后台或被系统挂起时，Noise 握手失败后的自动重连如何处理？
- 选项与取舍：A：后台不保持长连接、不执行持续重连；回到前台后从退避状态重新发起一次健康检查和完整 Noise 握手（推荐，符合 iOS 生命周期和省电约束）；B：允许后台短时间继续重连，恢复更快但受 iOS 后台执行限制；C：后台完全不记忆退避状态，回前台总是立即重试，体验直接但可能造成重复请求。
- 用户回答原文：选择 A。
- 证据：PRD FR-003 要求恢复连接后重新健康检查和同步；PRD 7.3 禁止手机持久化业务队列和自动重发任务，但未规定后台连接生命周期。
- 结论：Phone 进入后台或被系统挂起后，不保持 Noise 长连接，也不执行持续自动重连；回到前台时保留内存中的退避状态，重新发起一次健康检查和完整 Noise 握手。不得把业务正文、待发送队列或自动重发任务持久化来支持后台恢复。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Phone 生命周期与 Harness 场景。
- 未决事项：网络状态触发、退避状态在 App 被终止后的处理、后台通知/唤醒边界。
- 回滚条件：iOS 系统限制或能耗证据不支持所选策略时，重新开放本条。
- 状态：用户已确认（后台生命周期）；网络触发/终止态处理待完成

### Q-SV-2026-030：网络状态变化后的重连触发

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD FR-003
- 用户原文：「a」
- 需要确认的问题：Phone 观察到网络从不可用恢复为可用时，是否立即触发 Noise 重连，还是继续遵守当前退避计时？
- 选项与取舍：A：网络恢复只解除“不可连接”阻塞，不跳过当前退避；若当前没有退避计时则立即尝试，否则在计时到期后尝试，避免网络抖动造成连接风暴（推荐）；B：每次网络恢复都立即重置退避并重连，恢复快但容易在抖动网络下重复请求；C：忽略网络状态变化，只按固定退避计时器尝试，行为稳定但恢复可能延迟。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-028/029 已确认前台退避和后台生命周期；PRD FR-003 要求恢复连接后重新健康检查，但未规定网络监听触发。
- 结论：网络从不可用恢复为可用时，只解除“不可连接”阻塞，不跳过当前退避计时；若当前没有退避计时则立即尝试 Noise 重连，否则等待计时到期后尝试。网络恢复事件不得重置退避计数，避免网络抖动造成连接风暴。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Phone 网络状态与 Harness 场景。
- 未决事项：网络抖动去抖窗口、网络类型变化处理和网络状态 Spike 证据。
- 回滚条件：Spike 证明所选触发策略影响恢复率或造成 Relay 压力时，重新开放本条。
- 状态：用户已确认（触发策略）；去抖/类型处理待完成

### Q-SV-2026-031：网络恢复事件去抖窗口

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD FR-003
- 用户原文：「1」
- 需要确认的问题：网络恢复事件在多长去抖窗口内合并，避免 Wi-Fi/蜂窝切换或短暂抖动触发重复重连？
- 选项与取舍：A：采用 `1 秒` 去抖窗口；窗口内只保留最后一次“网络可用”事件，退避计时仍按 Q-SV-2026-030 执行（推荐，简单且能过滤短抖动）；B：采用 `5 秒` 去抖窗口，抖动过滤更强但恢复反馈更慢；C：不做应用层去抖，完全依赖系统网络状态事件。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-030 已确认网络恢复不跳过退避；PRD FR-003 要求恢复连接后健康检查，但未规定去抖窗口。
- 结论：网络恢复事件采用 `1 秒` 去抖窗口；窗口内只保留最后一次“网络可用”事件，退避计时继续按 Q-SV-2026-030 执行，不因去抖或网络恢复重置退避计数。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Phone 网络状态与 Harness 场景。
- 未决事项：网络类型切换边界和去抖/恢复 P95 Spike 证据。
- 回滚条件：实测去抖导致恢复 P95 不达标时，按证据重开本条。
- 状态：用户已确认（去抖窗口）；网络类型/Spike 证据待完成

### Q-SV-2026-032：网络类型切换处理

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD FR-003
- 用户原文：「a」
- 需要确认的问题：Wi-Fi、蜂窝网络或网络接口发生切换时，Phone 是否主动关闭当前 Noise 会话并重新握手？
- 选项与取舍：A：网络类型切换视为当前传输会话可能失效；关闭旧 WSS/Noise 会话，沿用现有退避和 `1 秒` 去抖规则重新建立链路（推荐，避免在旧路径上继续发送）；B：尽量复用现有会话，仅在发送失败后重连，减少握手但依赖底层路径迁移；C：网络类型切换不做特殊处理，完全由普通超时和退避处理。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-030/031 已确认网络恢复触发和去抖；PRD FR-003 要求恢复连接后重新健康检查，未规定接口切换语义。
- 结论：Wi-Fi、蜂窝网络或网络接口切换视为当前传输会话可能失效；Phone 关闭旧 WSS/Noise 会话，沿用现有退避和 `1 秒` 网络恢复去抖规则重新建立链路，不在旧路径上继续发送业务密文。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Phone 网络切换与 Harness 场景。
- 未决事项：切换检测边界、旧会话关闭错误映射、最大尝试次数和网络切换 Spike 证据。
- 回滚条件：网络切换 Spike 证明强制重握手影响恢复指标时，重新开放本条。
- 状态：用户已确认（网络切换策略）；边界/上限/Spike 证据待完成

### Q-SV-2026-033：自动重连最大尝试次数与封顶

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD FR-003
- 用户原文：「a」
- 需要确认的问题：前台自动重连在指数退避达到 `16 秒` 后是否设置最大尝试次数或最大封顶时长？
- 选项与取舍：A：不设置硬性最大次数，只将单次退避封顶为 `16 秒`；前台保持按上限周期尝试，进入后台即暂停（推荐，避免长时间断开后必须用户手动恢复）；B：连续失败 5 次后暂停自动重连，等待用户或网络状态事件重新触发；C：连续失败 10 次后暂停自动重连，降低 Relay 压力但恢复更慢。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-028/029/030/031/032 已确认退避、后台、网络恢复和接口切换语义；PRD FR-003 要求恢复连接后健康检查，但未规定最大次数。
- 结论：前台自动重连不设置硬性最大尝试次数；单次退避封顶为 `16 秒`，前台持续按该上限周期尝试，进入后台即暂停。成功握手后按既有规则清零退避计数；容量和限流约束仍需 Relay Spike 验证。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Phone/Relay 压力场景。
- 未决事项：Relay 限流/容量约束、后台恢复触发边界和持续尝试的压力证据。
- 回滚条件：容量 Spike 证明持续尝试会影响 Relay 稳定性时，重新开放本条。
- 状态：用户已确认（尝试次数策略）；Relay 限流/容量证据待完成

### Q-SV-2026-034：Relay 对重连握手的限流边界

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD 7.3
- 用户原文：「a」
- 需要确认的问题：Relay 是否需要针对同一 `pairing_id` 的重连握手设置独立限流，避免前台持续重连放大 Relay 压力？
- 选项与取舍：A：Relay 对每个 `pairing_id` 维护握手尝试限流，超限返回脱敏 `RATE_LIMITED`，不建立离线队列；具体阈值由容量 Spike 冻结（推荐）；B：只依赖 Phone 侧 `16 秒` 退避，不在 Relay 增加 pairing 级限流；契约简单但无法防止多端或异常客户端放大压力；C：全局限流，不区分 pairing，运维简单但容易误伤其他配对。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-033 已确认前台无限次尝试但单次退避封顶；PRD 7.2 要求 Relay 不建立离线业务队列，7.3 要求拒绝超限请求，但未规定 pairing 级握手限流。
- 结论：Relay 对每个 `pairing_id` 维护独立的握手尝试限流；超限对端返回脱敏错误 `RATE_LIMITED`，Relay 不建立离线业务队列。具体时间窗、桶容量和全局保护阈值由容量 Spike 冻结。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Relay Transport Schema、容量 Spike 与 Harness 场景。
- 未决事项：时间窗/桶容量、`RATE_LIMITED` 重试提示语义和全局保护阈值。
- 回滚条件：容量 Spike 证明 pairing 级限流无法改善稳定性或影响恢复率时，重新开放本条。
- 状态：用户已确认（限流边界）；阈值/Spike 证据待完成

### Q-SV-2026-035：Relay pairing 握手限流参数

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD 7.3
- 用户原文：「a」
- 需要确认的问题：同一 `pairing_id` 的握手限流采用哪一个初始时间窗和允许次数？
- 选项与取舍：A：`60 秒`窗口最多允许 `5 次`握手尝试，超限返回 `RATE_LIMITED`；窗口自然过期后恢复（推荐，作为容量 Spike 的初始假设）；B：`60 秒`窗口最多 `10 次`，恢复更快但保护更弱；C：不预设参数，完全由容量 Spike 测量后再冻结。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-034 已确认 pairing 级限流和 `RATE_LIMITED`；Phone 前台重连退避已封顶 `16 秒`，但 PRD 未规定 Relay 参数。
- 结论：同一 `pairing_id` 的握手限流初始参数为 `60 秒`窗口最多 `5 次`握手；超限返回 `RATE_LIMITED`，窗口自然过期后恢复。该值作为容量 Spike 初始假设，最终参数必须由 Relay 容量/压力证据校准。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Relay 容量 Spike 与 Harness 压力场景。
- 未决事项：全局保护已由 Q-SV-2026-036/037 确认边界和初始基线；容量 Spike 最终校准、Caddy/应用层职责边界和 `RATE_LIMITED` 重试提示语义待完成。
- 回滚条件：容量证据表明初始参数影响恢复率或 Relay 稳定性时，按证据重开本条。
- 状态：用户已确认（初始参数）；容量校准/全局阈值待完成

### Q-SV-2026-036：Relay 全局保护阈值

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD 7.3
- 用户原文：「a」
- 需要确认的问题：除 pairing 级握手限流外，Relay 是否需要全局握手并发/速率保护？
- 选项与取舍：A：增加全局保护阈值，超限统一返回 `RATE_LIMITED`；具体并发数和速率由容量 Spike 冻结，作为服务稳定性最后一道保护（推荐）；B：只做 pairing 级限流，不做全局保护，避免全局阈值误伤但无法防止大量 pairing 同时重连；C：由 Caddy/云负载层限流，Relay 应用层不定义公开语义。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-034/035 已确认 pairing 级限流及初始参数；PRD 7.3 要求拒绝超限请求，但未规定全局保护层。
- 结论：Relay 增加全局握手并发/速率保护；超限统一返回脱敏错误 `RATE_LIMITED`。具体全局并发数、速率窗口和 Caddy/应用层职责边界由容量 Spike 冻结，且不得替代 pairing 级限流。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Relay 容量 Spike、运维门禁。
- 未决事项：全局保护初始基线已由 Q-SV-2026-037 确认；Caddy 与应用层职责边界及容量 Spike 证据待完成。
- 回滚条件：容量 Spike 证明全局保护阈值导致正常恢复率下降时，重新开放本条。
- 状态：用户已确认（全局保护边界）；初始基线已由 Q-SV-2026-037 确认，职责/Spike 证据待完成

### Q-SV-2026-037：Relay 全局保护初始参数

- 日期：2026-08-24
- 关联需求：需求确认台账 D4、PRD 7.3
- 用户原文：「a」
- 需要确认的问题：Relay 全局握手保护采用哪一个初始并发/速率参数作为容量 Spike 基线？
- 选项与取舍：A：全局最多 `100` 个并发握手、每 `60 秒`最多 `300` 次握手尝试；超限返回 `RATE_LIMITED`（推荐，作为单实例容量 Spike 初始假设）；B：全局最多 `500` 个并发、每 `60 秒`最多 `1000` 次，恢复更快但保护更弱；C：不预设参数，完全由容量 Spike 测量后再冻结。
- 用户回答原文：选择 A。
- 证据：Q-SV-2026-036 已确认全局保护和 `RATE_LIMITED`；Q-SV-2026-035 已确认 pairing 级 `60 秒/5 次` 初始参数；Relay 技术栈为单台阿里云 ECS，容量仍待 E3 Spike。
- 结论：全局握手保护采用最多 `100` 个并发握手、每 `60 秒`最多 `300` 次握手尝试的初始基线；超限统一返回 `RATE_LIMITED`。该基线仅用于单实例容量 Spike，最终参数及 Caddy/应用层职责边界须由压力证据冻结。
- 影响文件：两端 D4 台账、两端架构基线、两端 `harness/契约/integration-profile.yaml`、Relay 容量 Spike、运维门禁。
- 未决事项：单实例规格、全局参数最终校准、Caddy/应用层职责边界和 Spike 证据。
- 回滚条件：容量 Spike 证明初始参数影响恢复率或实例稳定性时，按证据重开本条。
- 状态：用户已确认（初始基线）；容量校准/职责边界/Spike 证据待完成
