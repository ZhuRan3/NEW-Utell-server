# 需求确认台账

> 建立日期：2026-08-23
> 用途：把所有需求与选型拆成可单独确认的条目，逐项与用户确认。每条确认后按 `开发规则规范/01-需求对齐与Q&A规范.md` 在 `需求问答记录.md` 留下完整 Q&A，再回写本台账状态。
> 状态口径：待确认 / 用户已确认 / 暂定建议 / 已否决 / 已变更

## A. 链路与数据归属（跨端共同，两端台账一致）

| 编号 | 条目 | 对应 PRD | 功能对应 | 状态 |
| --- | --- | --- | --- | --- |
| A1 | 四层链路：手机 ↔ Relay ↔ PC Connector ↔ Utell Pi | 0.1/0.2 | 全链路 | 用户已确认（PRD 依据） |
| A2 | 数据权威在 Connector；手机业务数据仅存内存，禁持久化 | 0.2/2.2/7.3 | 提交、时间线、诊断 | 用户已确认（PRD 依据） |
| A3 | 应用层 E2EE 在端点终止；Relay 只见密文、不持密钥 | 0.2/7.3 | 传输安全 | 用户已确认（PRD 依据） |
| A4 | 一用户配对一台电脑 + 一台手机；不多设备不共享 | 2.3 | 配对、账号 | 用户已确认（PRD 依据） |
| A5 | Relay 无离线队列；未即时投递即丢弃并报错 | 7.2/FR-008 | 提交失败语义 | 用户已确认（PRD 依据） |
| A6 | Relay ACK ≠ 电脑接收/提交/投影 | 2.1/7.4 | 状态展示 | 用户已确认（PRD 依据） |

## C. 云端 Relay 需求

| 编号 | 条目 | 对应 PRD | 功能对应 | 状态 |
| --- | --- | --- | --- | --- |
| C1 | Relay 职责面：身份、配对、公钥登记、撤销、密文路由、ACK、重连、限流、最小脱敏审计 | FR-008 | 服务整体 | 用户已确认（PRD 依据） |
| C2 | 审计元数据范围（pairing 哈希、时间戳、大小区间、投递结果、错误类别）与 30 天留存 | 7.2 | 审计与合规 | 用户已确认（14 天） |
| C3 | 稳定错误码体系（PAIRING_INVALID/RELAY_UNREACHABLE/ROUTE_NOT_DELIVERED 等） | 7.4 | 端点可见错误 | 用户已确认（PRD 依据） |
| C4 | Connector 健康上报节奏（建议 10s）与手机 READY 判定时效（建议 30s） | 5.2/FR-003 | 链路状态 | 用户已确认 |
| C5 | 单主 Connector 约束与撤销/replacement 原子性 | FR-001/AC-018 | 配对管理 | 用户已确认（PRD 依据） |
| C6 | 进程重启语义：端点重新认证、不恢复未投递正文 | FR-008/AC-020 | 可靠性 | 用户已确认（PRD 依据） |
| C7 | 发布门禁：无法解密证明、重放/篡改/限流/撤销证据 | 7.3/9.2 | 发布 | 用户已确认（PRD 依据） |

## B. 手机端需求（详见 phone 仓库台账，此处仅交叉引用）

配对体验、READY 门禁、提交状态机、卡片交互等在 NEW-Utell-phone 台账维护；涉及 Relay 可见行为的（连接节奏、错误码消费）与本端 C3/C4 联动确认。

## D. 技术选型

| 编号 | 条目 | 关联决策包 | 状态 |
| --- | --- | --- | --- |
| D1 | 首发平台范围（仅 iOS / iOS+Android 同期） | TS-PH-2026-001（phone 仓库） | 用户已确认（仅 iOS） |
| D2 | 手机 UI 技术栈与最低系统版本 | TS-PH-2026-001（phone 仓库） | 用户已确认（SwiftUI + iOS 17+） |
| D3 | Relay 语言/边缘代理/元数据存储/云服务商 | TS-SV-2026-001 | 用户已确认（Go+Caddy+SQLite+阿里云） |
| D4 | E2EE 协议套件与跨端测试向量（需 Painfox 共同接受） | 待建 | 待确认 |
| D5 | 账号认证方式与 Provider | 待建 | 待确认 |

## E. 指标与发布

| 编号 | 条目 | 对应 PRD | 状态 |
| --- | --- | --- | --- |
| E1 | P0 指标阈值（配对 ≥90%、接收 ≥99%、P95 ≤5s/≤15s、解析 ≥95%、原文残留 0） | 1.3 | 待确认 |
| E2 | 发布顺序、灰度与回滚策略 | 9.1 | 用户已确认（PRD 依据） |

## 确认顺序建议

A（链路骨架）→ C（本端需求）→ D（选型）→ E（指标发布）。B 类在 phone 仓库同步进行。每确认一条：更新本台账状态 → 写 Q&A 记录 → 回写架构基线/契约文档。
