# D4 冻结前决策清单

## 当前建议

- Pattern：`XX`
- 完整协议名：`Noise_XX_25519_ChaChaPoly_SHA256`
- 角色：手机 initiator；Connector responder
- 首发默认：不启用 IK、Noise Pipes、XXfallback
- 状态：Pattern/角色已确认；Phone/Rust snow 本机 Spike 已完成，Connector 共同验收待完成

## 必须共同确认

1. 两端静态密钥的生成、存储、轮换、撤销和指纹格式。
2. token 的 5 分钟有效期、一次性消费、重放拒绝和 pairing 绑定字段。
3. `prologue` 是否使用；如使用，具体字节串和版本策略。
4. 握手 payload 是否为空；首发禁止在身份确认前承载业务原文。
5. handshake message 的封装字段、长度上限、超时和错误码。
6. transport nonce、方向键、rekey 触发和重建握手规则。
7. 公钥变更时是否强制回到 UNPAIRED 并重新扫码。
8. 固定向量：双方每条消息、hash、transport 密文、失败样本。

## 通过门禁后才能更新

- 两端 `harness/契约/integration-profile.yaml` 的 `e2ee_handshake_and_vectors` 从 `pending` 改为 `approved`。
- Phone Noise Spike 已写入原始结果与失败样本，Connector 共同向量验收仍待完成。
- Connector 侧实现使用同一协议名、角色和向量。
- Relay 继续只转发密文，不解析 Noise payload，不保存端点私钥。
